// At the top of the file, or in a separate models/ directory
import 'dart:io';

class TradeScreenshot {
  final File file;
  final String timeframe;

  TradeScreenshot({required this.file, required this.timeframe});
}
